{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        "players": {
          "$uid": {
            ".read": true,
            ".write": "auth != null && auth.uid == $uid"
          }
        }
      }
    }
  }
}
